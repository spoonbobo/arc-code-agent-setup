---
description: Run a sweep of an ARC-AGI game with different seeds
---

Run the same ARC-AGI game multiple times with different random seeds to test consistency.

Default: 5 runs, 10 steps each

If no game ID is provided, use `ls20-cb3b57cc` as default.

Use the arc_run_sweep tool to run the sweep and report results.
